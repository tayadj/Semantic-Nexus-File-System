#include "create.h"
#include <stdio.h>

int create_file(const char* id, const char* data, const char* metadata) {

    printf("Creating file with id: %s\n", id);
    printf("Data: %s\n", data);
    printf("Metadata: %s\n", metadata);

	return 0;
}